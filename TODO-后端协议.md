# 待办（TODO）

## 后端协议待办（需后端同学重部署）

- [ ] **视线诱导（gaze-guidance）技能描述未同步**：技能已重做为主动技（每日一次，
      先选被诱导者、再选诱导对象，被诱导者发言必须提及诱导对象），但
      `server/gameProtocol.mjs` 的 `PUBLIC_SKILLS` 白名单仍为旧文案
      「视线诱导：存活时，其他人的正常与伪造发言必须提及自己。」——
      当前白名单未改（后端 0 改动），UI/播报/提示词展示的仍是旧描述，
      与新机制不完全一致。待后端同学确认后同步新描述并重部署。
- [ ] 魔女技「洗脑」描述更新（旧文案「发言前指定当天的怀疑焦点」与重做后的
      语言洗脑机制不一致，已报备，仍待后端同步）
