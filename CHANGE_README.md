# 八人局改动说明

本文面向原作者和代码审阅者，解释本分支相对上游仓库 [liuziheng20091106/witch-wolfgame](https://github.com/liuziheng20091106/witch-wolfgame) 当前 `main`（提交 `be6b3c3`）仍然有效的改动。

说明按“实际反馈 → 原因 → 处理 → 边界 → 验证”组织。上游已经合并的修复、纯换行差异、本机安装造成的锁文件漂移，以及只存在于浏览器中的模型/API Key 设置，均不算作本分支成果。

## 一、用户反馈与处理结果

### 1. 狼人首轮没有依据却主动带票

**实际反馈**

第一轮公开信息不足，其他人甚至准备弃票时，狼人 AI 会随机投出第一票，把自己推成焦点位。这不是“狼人不能投票”，而是决策没有参考已经形成的票型，也没有在无依据时合理使用弃票。

**原因**

上游本地 fallback 对多数目标决策共用随机候选逻辑；远程 AI 虽能看到公开事件，但请求中没有结构化的当前投票列表，只能从自然语言时间线自行猜测票型。

**处理**

- `src/ai/fallback.ts` 为首轮 `vote` 增加专门策略：先统计 `currentVotes`；已有领先目标时从合法领先目标中确定性选择；完全没有非弃票时，谨慎度不低于激进度且允许弃票的角色选择弃票，否则才从合法候选中选择。
- `src/ai/prompts.ts` 把 `currentVotes` 作为结构化字段发送给模型。
- `shared/gamePromptContract.js` 明确要求投票参考真实票型、无依据时不要只为制造票型随机点人，也不因狼人身份套用固定投票模式。
- `server/gameProtocol.mjs` 校验 `currentVotes` 的投票者、目标和轮次，防止前后端契约不一致。

**边界**

没有在 reducer 中强制狼人弃票，也没有禁止狼人首票。远程模型仍可基于公开发言和性格主动投票；改动只消除“统一随机选人”造成的无依据行为。

**验证**

`npm run test:eight-player` 检查无前票和已有票型两种 fallback 路径；`npm run test:backend` 的有效请求样例包含 `currentVotes`。

### 2. 八人局、每天两轮发言和平票辩解

**实际反馈**

六人局流程较短，希望改成固定八人；每名玩家按顺序发言后再进行一轮随机顺序自由发言；两人平票时应先让候选人各自补充发言，三人以上平票则当天无人出局。

**原因**

上游的玩家编号、职业池、提示词数量上限和部分存档校验以六人为固定前提；每天只有 `speeches` 一轮，任何最高票平局都会直接进入重投。

**处理**

- 新局固定为 8 人：狼人×2、预言家×1、女巫×1、猎人×1、村民×3。
- 第一轮继续使用开局固定顺序；每天黎明用现有种子 RNG 生成 `freeSpeechOrder`，进入 `free-speeches` 第二轮。
- 发言和禁言事件写入 `speechRound`：1 为第一轮，2 为自由发言，3 为两人平票补充发言。
- 两人最高票并列时进入 `runoff-speeches`，按第一轮顺序各发言一次后重投。
- 三人或以上最高票并列时直接进入日结算，不放逐任何人。
- UI 只增加新阶段名称和八人入场文案，没有改动页面结构。

**边界**

这是固定八人版型，不是动态人数系统，也不等于完成上游 Issue #70 的 14 人或 Issue #73 的自定义人数规划。第二轮顺序是确定性随机，不使用 `Math.random()`。

**验证**

`npm run test:eight-player` 检查固定种子可复现、两轮各发言一次、两人平票后发言再重投、三人以上平票无人出局；`npm run build` 检查新增阶段在类型和 UI 中完整覆盖。

### 3. 猎人死亡后开枪到底如何结算

**实际反馈**

八人局加入猎人后，需要明确猎人由 AI 或玩家操作时的开枪触发条件、是否必须开枪、与死亡回溯/遗言/胜负判断的先后关系。

**原因**

上游没有猎人职业，也没有死亡后继续产生强制目标决策的状态机。

**处理**

1. 猎人先按普通死亡流程被标记死亡并记录死亡来源。
2. 如果死亡回溯成功，本次死亡被撤销，不产生开枪。
3. 如果死亡保留，状态机会查找“已死亡但尚未开枪”的猎人，生成 `hunter-shot` 决策。
4. 开枪不能弃权，目标是其他所有存活单位。
5. 枪击目标通过统一 `resolveDeathBatch` 死亡，因此可以继续触发另一名猎人或死亡回溯。
6. 所有猎人开枪处理完后，才继续遗言和胜负判断。

开枪事件本身用作去重标记，因此同一名猎人不会重复开枪。夜间死亡停留在 `night-resolution`，白天死亡停留在 `day-resolution`，不会另造一套死亡系统。

**边界**

规则按需求实现为“必须开枪”；没有增加不开枪选项。当前固定版型只有一名猎人，但队列逻辑可正确处理灵魂交换等效果造成的连锁猎人死亡。

**验证**

`npm run test:eight-player` 检查猎人死亡后产生强制决策、目标死亡和公开开枪事件。现有领域 smoke 继续覆盖统一死亡与回溯路径。

### 4. 玩家狼人有时只能建议，最终夜刀被 AI 队友决定

**实际反馈**

“加入一个席位”模式中，玩家是狼人时，有时自己拍板，有时只是给建议；AI 队友还会反复袭击疑似每天自守的治愈/预言家目标，实际控制感很差。

**原因**

上游最终袭击者固定取存活狼人列表的第一名。玩家座位不是第一名时，只参与前面的狼人建议阶段，最终目标由更靠前的 AI 狼人决定。

**处理**

`src/domain/engine/reducer.ts` 在 `wolf-decision` 阶段优先选择“参与模式中的存活玩家狼人”为最终行动者；其他狼人仍依次提交私下建议。玩家不是狼人或已经死亡时，继续由首名存活狼人决定。

**边界**

这项改动稳定了玩家的最终决定权，但没有修改 AI 狼人的目标评价，也没有解决观战模式下 AI 反复刀长期受保护目标的问题。治愈 AI 连续自守同样仍待单独改进。

**验证**

`npm run test:eight-player` 搜索“玩家狼人座位晚于队友”的固定种子，并断言最终 `wolf-decision.actorId` 仍是玩家。

### 5. 猎人等存活者错误地说自己知道被狼刀后获救

**实际反馈**

参与模式中，AI 操作的猎人在夜间存活后直接说自己“被救下”。玩家并未收到这类私密信息，AI 却把“还活着”当成“确定知道自己被袭击并获救”。别人公开发银水可以讨论，但不能自动变成本人的确定记忆。

**原因**

领域层原本只把女巫救人事件给女巫本人，目标并没有收到该私密事件；问题主要来自模型依据存活结果自行脑补。提示词此前没有明确区分“可以分析别人的公开说法”和“本人亲历的私密事实”。

**处理**

- 系统提示明确：存活不表示知道自己曾被袭击、被解药救下或被治愈保护；没有私密事件证据时不能把它断言为自身事实。
- 同时保留社交推理空间：可以引用、质疑或分析其他人的公开“银水”说法。
- `privateEvents` 在 AI 负载中统一加 `【你亲历的私密事实】`，把真正属于该行动者的私密行动与普通公开推理分开。

**边界**

没有把所有救人/保护信息公开，也没有禁止角色撒谎。这里修正的是模型的知识边界；远程生成仍有概率违反提示，领域层不会根据自然语言内容判定真假。

**验证**

`npm run test:debug-report` 检查系统提示包含存活/被救知情边界，并检查私密事件使用确定事实标记。

### 6. 角色把基础职业能力和公开魔女技混为一谈

**实际反馈**

出现过“公开技能是魔女杀手，所以不可能有解药”的常识错误。另有角色对自己究竟持有什么技能、何时能用、目标是谁、谁会知道结果等认识模糊。

**原因**

上游已经实现“基础职业与魔女技相互独立”的基础说明，但 `publicSkills`、角色当前实际技能和职业资源在长提示词中仍容易被模型混淆；部分技能只有简短策略建议，没有把完整效果和知情范围写入当前行动者的技能字段。

**处理**

- 明确女巫解药/毒药、预言家查验、狼人袭击、猎人开枪属于基础职业能力或资源，与公开魔女技并存。
- `actor.skill` 明确写成“你当前持有的魔女技”，只有该字段表示当前实际技能。
- `publicSkills` 明确只表示开局公开的默认技能归属，不能用于否定他人的基础职业能力，也不能当成自己的技能。
- 为 14 项魔女技逐项补充：发动时机、是否可保留、合法目标、消耗、可阻止/不可阻止效果、公开或私密知情者以及特殊失败情况。

**边界**

这不是首次实现职业/魔女技分层；上游 PR #11 已有基础规则。本分支的增量是把所有技能的具体规则和例外送入当前持有者的 `actor.skill`，并强化职业资源的解释。

**验证**

`npm run test:debug-report` 遍历 14 项技能，检查技能名、描述和专属认知进入 `actor.skill`，长度不超过协议上限，并逐项通过后端提示词校验。

### 7. 千里眼观看者否认自己看过直播

**实际反馈**

角色亲自选择观看千里眼直播并暴露职业后，后续却说“自己没看过直播”。这不是公开信息推理错误，而是否认了本人刚执行的私密行动。

**原因**

上游已经为观看者和开播者记录了私密反馈，但旧提示词没有把这类记录明确标成不可混淆的本人确定事实。

**处理**

- 所有当前行动者可见的私密事件统一标记为 `【你亲历的私密事实】`。
- 系统提示明确：观看者确定知道自己是否观看；开播者确定知道哪些人观看及看到的职业。
- 除非角色是在有目的地说谎，否则不得否认这些本人亲历事实。

**边界**

千里眼的观看流程、观看者/开播者私密反馈本身已由上游 PR #37 实现，本分支不把它重复算作新机制；这里只强化 AI 对已有事实的记忆一致性。

**验证**

`npm run test:debug-report` 检查系统提示和私密事实前缀，并验证全部提示词仍通过后端协议。

### 8. 幻视轨迹被误当成完整职业证明

**实际反馈**

玩家关注两个边界：狼刀被解药挡下后是否还存在袭击轨迹；以及仅剩一狼时，如果幻视只看到某人“被查验”而没显示袭击轨迹，能否直接证明她不是狼。

**原因**

幻视展示的是概率得到的脱敏行动轨迹，不是完整职业日志。狼人袭击意图与最终是否死亡是两件事：解药会阻止死亡，但不会把已经发生的袭击行动改写成“从未行动”；反过来，某次幻视没有展示袭击轨迹，也不能作为排除狼人的硬证明。

**处理**

本分支没有重写上游的幻视轨迹生成逻辑；只在技能说明和持有者认知中明确“概率、脱敏、已触碰目标不可重复、漂浮会隐藏痕迹”，并补充“未显示某类轨迹不等于未行动或不是对应职业”的推理边界。

**边界**

因此：看到袭击轨迹是有效信息；没有看到袭击轨迹不是反向铁证。狼刀是否被救与“狼人是否发起过袭击”也不能混为一谈。

**验证**

`npm run test:debug-report` 检查幻视的专属认知文本进入实际持有者提示词。轨迹事件生成本身沿用上游测试。

### 9. 视线诱导漏提对象导致 AI 决策暂停

**实际反馈**

模型发言没有包含“必须提及”的角色名时，请求通过 JSON 校验但随后因规则不满足而暂停，频繁打断自动游戏。

**原因**

远程模型并不总能服从必提要求；旧解析只校验 JSON 结构，没有在合法发言进入 reducer 前补齐遗漏文字。

**处理**

`src/ai/schemas.ts` 在发言 JSON 解析成功后检查 `requiredMention` 和座位标签。两者都没有出现时，把角色名追加到发言末尾；若原文已接近 100 字，先确定性截短，为补入名称留出长度。

**边界**

只处理当前待决策中明确存在的 `requiredMention`，不会根据其他人的发言自动推断诱导对象，也不会改写玩家手动提交的 UI 校验规则。

**验证**

`npm run test:debug-report` 覆盖缺名自动补入、已有座位号不重复补入、100 字上限三种情况。

### 10. 运行观感慢与 460 毫秒间隔

**实际反馈**

运行八人局后感觉明显变慢，并询问控制器中的 460 毫秒是什么。

**原因**

`src/app/useGameController.ts` 的 460 毫秒是“当前没有待处理决策时，自动推进一次状态机”的展示间隔，不是模型推理耗时。八人双轮发言后，单日常规发言从六人版的最多 6 次远程请求增加到最多 16 次，再加技能、投票和平票请求，真实总耗时主要由远程模型响应次数和速度决定。

**本次处理**

没有修改 460 毫秒，也没有把动画/流程间隔打包成 PR 改动。此前切换模型属于浏览器本地配置，不写入源码、不进入交付包。贸然缩短间隔只会减少事件展示时间，不能解决远程 AI 请求慢。

**待提升**

若后续专门优化性能，应单独测量“状态推进等待”和“模型请求等待”，再决定是否并行无依赖决策、减少提示词或调整展示节奏。本 PR 不顺手引入这些行为变化。

## 二、逐文件改动说明

| 文件 | 原问题/改动原因 | 具体改法 | 行为影响与验证 |
|---|---|---|---|
| `shared/gamePromptContract.js` | 共享契约仍固定六人，缺猎人、新阶段、实时票型和完整认知边界 | 扩展到 8 座位和八人职业池；加入 `free-speeches`、`runoff-speeches`、`hunter-shot`、`currentVotes`；调整人数/发言上限；完善系统提示和 14 项公开规则描述 | 前后端共用同一八人协议；由 build、debug-report、backend smoke 验证 |
| `src/domain/model.ts` | 状态无法区分六/八人，不能保存第二轮顺序和猎人枪击来源 | 增加 `seatCount`、`freeSpeechOrder`，死亡来源加入 `hunter-shot` | 为状态机和存档提供最小必要字段；由 TypeScript build 和八人 smoke 验证 |
| `src/domain/engine/createGame.ts` | 新局角色抽取、知识表和提示文案写死六人 | 按 `PLAYER_IDS.length` 选择角色；初始化八人职业、动态知识表、两套发言顺序和 `seatCount: 8` | 固定种子创建结果可复现；八人 smoke 检查人数和职业数量 |
| `src/domain/engine/reducer.ts` | 缺双轮发言、平票发言、猎人提交、稳定玩家夜刀权 | 每天黎明洗牌第二轮；按轮次推进发言；新增两人/多人平票分支；提交猎人枪击；参与模式优先玩家狼人拍板 | 核心状态机行为由八人 smoke 覆盖，构建检查阶段联合类型完整性 |
| `src/domain/engine/night.ts` | 死亡流程不能在遗言/胜负前插入猎人强制决策 | 查找已死未开枪猎人，以公开枪击事件去重；夜间死亡结算后先回溯、再开枪、再遗言/胜负 | 枪击继续走统一死亡批次，可连锁；八人 smoke 覆盖基本路径 |
| `src/domain/skills/speechSkills.ts` | 时间线无法区分两轮和平票发言 | `publishSpeech` 接收并写入 `speechRound`，伪造片段事件同样保留轮次 | AI 能正确看到当前日多轮发言；由 build 和八人 smoke 验证 |
| `src/domain/skills/postGame.ts` | 注释写死赛后玩家为 0→5，与动态牌局不符 | 只修正注释为“当前牌局普通玩家” | 不改变运行行为，避免审阅者误解六人假设 |
| `src/ai/fallback.ts` | 首轮投票沿用随机候选，造成无依据带票 | 新增首轮投票统计和性格弃票分支，继续使用确定性 RNG | 已有票型会参考领先目标；无票型不再无条件投人；八人 smoke 验证 |
| `src/ai/prompts.ts` | 模型不知道发言轮次、结构化票型；私密事实和实际技能标识不够清晰 | 发言增加轮次；加入 `currentVotes`；私密事件加确定事实前缀；`actor.skill` 明确当前持有技能 | 直接对应投票、救人认知、千里眼否认和技能混淆反馈；debug-report smoke 验证 |
| `src/ai/schemas.ts` | 视线诱导漏词会让自动游戏暂停 | 解析发言后确定性补入必提姓名，并保持 100 字上限 | 只修远程 AI 合法响应的漏词；debug-report smoke 覆盖三种情况 |
| `src/domain/catalog/witchSkills.ts` | 上游只有部分简短使用建议，AI 对效果、例外和知情范围仍模糊 | 14 项技能逐项改成规则认知，拼入当前持有者的 `actor.skill` | 解决女巫药/公开技能混淆、本人行动否认等问题；逐项协议测试 |
| `src/storage/browserStorage.ts` | 新字段无法解析旧存档；进行中六人局不能继续八人规则 | 六/八人均可解析；补轮次字段；仅进行中的六人局追加猎人和村民，并同步晨间快照 | 已结束/赛后六人结果不改写；当前迁移为兼容补位，不是时间线重放 |
| `src/features/game/GameView.tsx` | UI 不认识三个发言阶段 | 增加第一轮、自由发言轮和平票候选发言名称 | 纯展示适配；build 验证 |
| `src/features/setup/SetupView.tsx` | 参与模式说明仍写六人 | 文案改为八人入场后分配职业 | 纯展示适配；build 验证 |
| `server/gameProtocol.mjs` | 后端只接受六人最终职业/公开技能，且不校验票型 | 接受八人新局和历史六人赛后数据；校验 `currentVotes` | 新前端请求可通过，历史赛后请求仍兼容；backend/debug-report smoke 验证 |
| `scripts/eight-player-smoke.mjs` | 缺少八人核心流程的集中回归测试 | 新增人数/职业、确定性、双轮发言、投票 fallback、玩家夜刀、平票和猎人测试 | 对应本分支最核心的领域增量；`npm run test:eight-player` |
| `scripts/ai-debug-report-smoke.mjs` | 既有测试未覆盖完整技能认知和视线诱导修复 | 遍历全部技能，校验提示词、后端协议、必提补全和长度限制 | `npm run test:debug-report` |
| `scripts/browser-storage-smoke.mjs` | 原测试只覆盖版本警告，没有验证六转八迁移边界 | 构造进行中和已结束的历史六人状态，分别断言扩展为八人和保持六人 | `npm run test:storage` |
| `scripts/backend-smoke.mjs` | 有效协议样例仍是六人且缺 `currentVotes` | 测试负载改为 8 人、8 项公开技能并加入空票型 | `npm run test:backend`；本机若缺 OpenSSL 会在生成临时证书前停止 |
| `package.json` | 新增 smoke 没有 npm 入口 | 增加 `test:eight-player` | 不增加依赖，不改变 `npm install`/`npm run dev` |
| `README.md` | 原说明仍以六人功能为主，缺本分支入口 | 更新八人功能摘要、运行方式，并链接本说明和 PR 草案 | 运行命令与上游保持一致 |
| `PR_DESCRIPTION.md` | 原作者需要可直接用于 PR 的独立说明 | 单独整理背景、有效增量、反馈来源、兼容性、测试和审阅顺序 | 不参与运行 |

## 三、旧存档迁移的精确边界

- 新存档写入 `seatCount: 8` 和 `freeSpeechOrder`。
- 旧存档没有 `seatCount` 时先按六人解析。
- 旧发言/禁言事件没有 `speechRound` 时视为第一轮。
- 仍在进行且确为 6 名玩家的存档追加 7 号猎人、8 号村民；角色从尚未使用的角色中按现有顺序选取。
- 新玩家获得自己的职业事实，所有玩家获得新增角色的公开默认技能事实。
- 若存在晨间回溯快照，同步追加两名玩家，避免回溯后重新变回六人。
- `ended`、`post-game` 或已有结果的六人局保持六人，不改写胜负和时间线。

这是一种继续游玩的兼容迁移。新增角色没有参与旧夜晚、旧发言和旧投票，因此不能称为严格历史重放。

## 四、没有算作本分支成果的内容

- 上游 PR #9：发言来源标注。
- 上游 PR #11：基础职业与魔女技的基础分层。
- 上游 PR #37：千里眼观看者/开播者私密反馈。
- 上游 PR #51：公开第一轮发言顺序。
- 上游 PR #54：私密事件使用具体角色名。
- 上游 PR #57：非法目标和女巫行动校验。
- 上游 Issue #58 对应的存档版本警告。
- Open PR #65 的动态角色演绎和提示词预算。
- 本机为解决 npm/Rolldown 安装问题进行的依赖重装；`package-lock.json` 已恢复上游版本。
- 浏览器中填写或切换的 API 端点、模型和 API Key；这些属于 `localStorage`，不会进入源码和 PR。
- 460/580 毫秒自动推进或本地策略展示延迟；本次没有修改。

## 五、仍待提升

- 治愈 AI 仍可能连续自守。
- 观战或玩家非狼人时，狼人 AI 仍可能重复袭击疑似长期受保护目标。
- 提示词能降低常识错误，但不能保证远程模型永远不在自然语言中犯错或撒谎。
- 幻视是部分、概率、脱敏轨迹；如需让 UI 明示“缺失不构成排除证据”，应作为独立产品改动讨论。
- 六转八迁移不重放旧时间线。
- 当前以 smoke 测试为主，尚缺所有死亡来源、回溯和连锁开枪组合的细粒度单元测试。
- 若上游先合并 PR #65，应在新提示词结构上重新移植 `prompts.ts` 和 `witchSkills.ts` 的认知增量，不能直接覆盖。

## 六、运行和验证

运行方式与上游一致：

```bash
npm install
npm run dev
```

验证命令：

```bash
npm run build
npm run test:eight-player
npm run test:domain
npm run test:storage
npm run test:debug-report
npm run test:backend
```

当前已通过前五项。`test:backend` 在本机因找不到 OpenSSL，未能进入测试断言；这是本机测试依赖阻塞，不是后端断言失败。

## 七、隐私和交付内容

- 交付源码不包含个人 API Key、本机绝对路径、聊天记录、浏览器 `localStorage`、证书、`.env` 或真实服务配置。
- 测试中的 `sk-live-super-secret-value`、`private-api.example.com`、`session-secret-must-not-export` 是固定假数据，用于验证脱敏，必须保留测试意义。
- 上游作者信息、仓库链接、许可证和版权联系方式属于原项目信息，予以保留。
- 交付包排除 `node_modules/`、`dist/`、`*.tsbuildinfo`。
- 不参与运行的上游原文件副本放在 `archive/upstream-be6b3c3/`，仅供对照和回退，不被任何 npm 脚本引用。
